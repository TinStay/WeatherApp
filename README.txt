#Weather App

This is a simple app that checks the weather in diffent cities around the world.

It is done in Python with the use of Tkinter library for the GUI.

The API is from OpenWeatherMap.

I have removed the API key in the python code.